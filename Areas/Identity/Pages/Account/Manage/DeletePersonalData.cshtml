@page
    <!--
    PROJETO DE PROGRAMAÇÃO - 50 HORAS
    FORMADOR: PAULO JORGE

    TEMA: WEB RPG CREATION
    FORMANDO: RODRIGO FERNANDES - Nº 13

    RAZOR VIEW PARA MOSTRAR LAYOUT BÁSICO PARA DELETAR DADOS PESSOAIS
    -->

    @model DeletePersonalDataModel
    @{
        ViewData["Title"] = "DELETAR DADOS PESSOAIS";
        ViewData["ActivePage"] = ManageNavPages.PersonalData;
    }

    <h3><b>@ViewData["Title"]</b></h3>
    <p></p>
    <div class="alert alert-warning" role="alert">
        <p>
            <strong>Excluir esses dados removerá permanentemente sua conta e isso não poderá ser recuperado.</strong>
        </p>
    </div>
    <p></p>
    <div>
        <form id="delete-user" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @if (Model.RequirePassword)
            {
                <div class="form-floating">
                    <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" />
                    <label asp-for="Input.Password" class="form-label"></label>
                    <span asp-validation-for="Input.Password" class="text-danger"></span>
                </div>
            }
            <p></p>
            <button class="w-100 btn btn-lg btn-danger" type="submit">Excluir dados e fechar minha conta</button>
        </form>
    </div>

    @section Scripts {
        <partial name="_ValidationScriptsPartial" />
    }
